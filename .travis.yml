language : node_js
node_js :
  - "stable"
cache:
  directories:
  - node_modules

script :
  - echo "Deploying!"

install :
  - npm install
  - npm run build
  - npm i -g firebase-tools
  - firebase -V
  - firebase deploy --token $FIREBASE_TOKEN
after_sucess:
  - echo "--End firebase--"
